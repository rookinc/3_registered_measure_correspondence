# Registered Measure Correspondence

Program 03 of the Quantum Relativity research program.

## Starting point

Program 02 closed the algebraic EPR assembly.

Its terminal interface is

    P_odd
      -> Gamma
      -> w_ab
      -> CHSH = 1 + 3/sqrt(5) > 2.

The finite construction derives a unique positive normalized algebraic measure

    w_ab = (1 + a b Gamma) / 4.

Program 03 does not reconstruct that result.

It begins from it.

## Primary question

How does a canonical positive algebraic measure become registered empirical
frequency?

The distinction to preserve is

    algebraic measure
      !=
    finite state count
      !=
    observed frequency.

Program 02 derives the first.

Program 03 investigates the bridge to the third.

## Motivation

The nontrivial Program-02 weights contain sqrt(5).

They therefore cannot be represented exactly as finite equal-count ratios.

A finite deterministic periodic orbit likewise produces rational limiting
visit frequencies.

The correspondence problem is therefore not solved by counting hidden source
states.

The apparatus must supply additional registered structure.

Possible mechanisms include:

- evolving finite history,
- repeated relational encounters,
- algebraic transition weights,
- nonperiodic registered dynamics,
- induced invariant measures,
- asymptotic frequencies,
- or another native finite process that does not collapse to static counting.

These are candidates, not assumptions.

## Required output

The program should determine whether finite registered mechanics supplies a
law of the form

    canonical algebraic weight
      -> registered repeated-trial frequency.

A successful construction must explain why the empirical frequency attached
to an outcome class converges to the algebraic measure already fixed by the
finite relational geometry.

## Attack conditions

The program fails if correspondence requires inserting the target frequencies
by hand.

It also fails if the only available mechanism is:

- finite equal-count hidden-state counting,
- setting-dependent source weighting,
- postselection after measurement settings are chosen,
- remote-setting dependence in local response rules,
- or an assumed Born frequency rule with no finite provenance.

## Program-02 interface

The imported algebraic facts are:

    Gamma(A,B)
      =
    Tr[P_odd (A tensor B)]

and, on traceless local observables,

    Gamma(A,B)
      =
    -1/2 Tr(AB).

Native analyzer geometry gives

    v = 1/sqrt(5).

For binary outcomes,

    w_ab
      =
    (1 + a b Gamma) / 4.

The frozen quartet gives

    CHSH
      =
    1 + 3/sqrt(5).

These are inputs to Program 03, not targets to be rediscovered.

## Result

Program 03 separates three questions that should not be conflated:

    What is the measure?

    Can that measure be realized as exact long-run registered frequency?

    Does the currently known native Thalean history apparatus generate
    that frequency history?

The first two are closed.

The third remains open at one explicit native-provenance interface.

## Finite-count obstruction

The nontrivial Program-02 weights are

    1/4 + sqrt(5)/20

and

    1/4 - sqrt(5)/20.

They are irrational.

Therefore they cannot be exact finite equal-count ratios.

Likewise, a deterministic map on a fixed finite state set is eventually
periodic, so every exact limiting visit frequency is rational.

Thus

    algebraic measure
      !=
    finite hidden-state count.

This is the Audit-001 obstruction.

## Finite rule and growing history

A finite rule does not imply a fixed finite global history.

The Fibonacci substitution

    A -> AB
    B -> A

contains no probability and no irrational constant, yet its growing registered
history has irrational limiting symbol frequencies

    f_A = (sqrt(5)-1)/2

and

    f_B = (3-sqrt(5))/2.

This establishes a mechanism class in which finite rules support irrational
asymptotic measure through unbounded registered history.

## Native spectral quadratic measure

The Program-02 algebra already contains the required measure.

For a binary analyzer pair define

    O = A tensor B.

Because O is a reflection,

    O^2 = I,

with complete orthogonal spectral projectors

    Q+ = (I+O)/2

and

    Q- = (I-O)/2.

The canonical joint source projector P_odd gives

    Gamma = Tr(P_odd O).

Hence

    Tr(P_odd Q+) = (1+Gamma)/2

and

    Tr(P_odd Q-) = (1-Gamma)/2.

Orienting the two classes by sign(Gamma) gives

    q_fav   = (1+|Gamma|)/2

and

    q_unfav = (1-|Gamma|)/2.

With the native value

    |Gamma| = 1/sqrt(5),

these become

    q_fav   = (5+sqrt(5))/10

and

    q_unfav = (5-sqrt(5))/10.

Because P_odd is rank one,

    P_odd = |psi><psi|,

the same weights are intrinsic squared component norms:

    Tr(P_odd Q) = ||Q psi||^2.

No cross outcomes are discarded.

No postselection is used.

The algebraic measure is therefore native at the operator level.

## Exact registered-frequency realization

Program 03 constructs the same irrational frequency history in two exact
coordinate descriptions.

### Mechanical residual form

Let

    mu = (5+sqrt(5))/10.

Use the deterministic rule

    r <- r + mu.

Whenever the unit boundary is crossed, emit one receipt and retain the
remainder.

Exactly,

    N_1(n) = floor(n mu)

and

    0 <= n mu - N_1(n) < 1.

Therefore

    |N_1(n)/n - mu| < 1/n,

so the registered frequency converges exactly to mu.

No random input or stochastic transition probability is required.

### Finite substitution form

Use

    A -> AB
    B -> A

followed by the finite receipt morphism

    A -> 1
    B -> 10.

Its asymptotic binary output frequencies are exactly

    (5+sqrt(5))/10

and

    (5-sqrt(5))/10.

No irrational state variable is required in this representation.

## Exact Sturmian identity

The two frequency constructions are not competing mechanisms.

They generate exactly the same infinite binary Sturmian history, up to one
receipt of phase.

Let

    alpha = (5-sqrt(5))/10.

In the Fibonacci fixed word, the kth B occurs at

    floor(k phi^2).

After the morphism B -> 10, the kth zero occurs at

    floor(k(phi^2+1)).

Since

    phi^2+1 = 1/alpha,

the zero positions are exactly

    z_k = floor(k/alpha).

The phase-aligned residual construction has the same zero-position law.

Thus the discrete morphic and algebraic carry descriptions define one exact
infinite registered history.

## Native carry structure

Prior Thalean receipt theory already supplies a discrete return-receipt
grammar.

For a lifted cycle generated by H with order 2n,

    tau = H^n

is the distinguished order-two return receipt.

For the four-phase case,

    H^4 = tau

and

    H^8 = 1.

Thus discrete wrap, retained receipt, and later full closure are native.

## Native path-sensitive history

Project 41 also supplies genuine path-sensitive registered continuation.

The local history class is

    h in {2,3}.

At each continuation junction two branches are admissible:

    preserve roles:
        h_next = 5-h

    swap roles:
        h_next = h.

These arrows form an exact transitive four-object registered-history
transformation groupoid.

Therefore the native apparatus contains more than a static state flip.

It contains composable history-sensitive continuation.

## Native provenance obstructions

Several apparently natural provenance routes are now excluded.

### Static native b

The native b action exchanges the two members of a registered-history fiber:

    r0 <-> r1.

It has order two.

Repeated application is period two and cannot generate an irrational-density
Sturmian history.

More generally, no single fixed automorphism of a finite carrier can generate
the required nonperiodic history.

### Orientation-blind row context

All 120 tested registered connector rows are orientation invariant.

The number whose row-local context distinguishes the two retained history
orientations is

    0.

Therefore the current connector row cannot choose the required continuation
branch.

### Existing path recurrence

The independently derived native history recurrence is

    kernel depth          1
    history action        2
    history word period   2
    registered surface    4.

The native phase line is C4 with full return period four.

This is genuine path-sensitive geometry, but it is still finite-periodic.

It is not the nonperiodic Sturmian history required for the exact irrational
frequency correspondence.

## Remaining interface

No native selector is presently derived that chooses the successive

    preserve roles

versus

    swap roles

branches in a nonperiodic history-bearing way.

Choosing those branches according to the desired Sturmian word would insert
the answer and is therefore forbidden.

The remaining native theorem gap is:

    derive a nonperiodic history-bearing selector
    or growing-history law

from independently native structure.

## What is closed

Program 03 has closed:

- the obstruction to finite equal-count interpretation,
- the distinction between finite rule and fixed finite global state,
- the native algebraic quadratic measure,
- an exact deterministic long-run frequency realization,
- an exact finite-rule realization of the same frequency,
- the infinite Sturmian equivalence of those two realizations,
- the native discrete carry grammar,
- the existence of native path-sensitive continuation geometry,
- the static-automorphism provenance obstruction,
- the row-local selector obstruction,
- and the insufficiency of the currently derived period-2 / period-4
  native history recurrence.

## What remains open

The exact algebraic measure is not missing.

The exact frequency mechanism is not missing.

A new probability law is not missing.

The sole unresolved correspondence interface is native provenance of the
nonperiodic registered history.

In compact form:

    native algebraic measure
      -> CLOSED

    exact frequency realization
      -> CONSTRUCTED

    native discrete carry grammar
      -> CLOSED

    native path-sensitive continuation grammar
      -> CLOSED

    native nonperiodic branch selector
      -> OPEN

## Program boundary

Do not continue searching this program for:

- another weighting law,
- another irrational-number generator,
- another hidden-state census,
- another static automorphism orbit,
- or another frequency construction.

Further progress requires genuinely new native mathematics that produces a
nonperiodic history-bearing selector or growing-history law.

Until such structure is derived, the exact Sturmian realization remains a
constructive correspondence witness rather than native empirical-frequency
provenance.

## Final status

    PROGRAM 03 REGISTERED MEASURE CORRESPONDENCE

    Algebraic measure:              CLOSED
    Constructive frequency bridge:  CLOSED
    Native carry grammar:           CLOSED
    Native path grammar:            CLOSED
    Native frequency provenance:    OPEN

Terminal frontier:

    exact Sturmian registered history
      <- constructive correspondence closed

    exact Sturmian registered history
      <- native nonperiodic provenance open
